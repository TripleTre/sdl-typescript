const video = require('../../out/video/video');
const expect = require('chai').expect;

// describe('测试各枚举常量', function() {
//   it('SDL_GLattr 是否正确', function () {
//     let glAttr = video.GLAttr;
//     for(let attr in video) {
//       if (video.hasOwnProperty(attr) && 1 * attr === attr) {
//         expect(video).to.equal(video.glGetAttribute(attr))
//       }
//     }
//   });
// });